"""Provider-independent domain value objects.

Organized by concern: ``source`` holds identities/provenance references shared
by every model, while model-specific packages (e.g. ``person``) hold only the
value objects for that model.
"""
